# dependencies
node_modules/
.pnpm-store/

# builds
dist/
.next/
out/
*.tsbuildinfo

# env — NEVER commit real secrets. Only .env.example is committed.
.env
.env.*
!.env.example

# turbo
.turbo/

# test / coverage
coverage/

# logs
*.log
npm-debug.log*

# os
.DS_Store

# infra — local-only mocks (apisix). Track mustang-mock: it must reach the VPS
# via git so its Dockerfile/compose can build there.
infra/*
!infra/mustang-mock/